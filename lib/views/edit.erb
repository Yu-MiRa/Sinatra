<html>
  <head>
    <title>Editer un potin</title>
  </head>
  <body>
    <h1>Editer le potin n°<%= params['id']%></h1>
    <p>Change ton potin via le formulaire ci-dessous </p>
    <form action ='/gossips/edit/<%= params['id']%>' method='post'>
      Balance ici le potin édité : <input type='text' name='gossip_content'/><br/>
      <input type='submit'/>
    </form>
  </body>
</html> 
